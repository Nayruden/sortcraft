# Gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.daemon=true

# Mod Properties (version-independent)
mod_id=sortcraft
mod_name=Sortcraft
mod_version=1.1.0
mod_group=net.sortcraft
mod_author=Nayruden
mod_description=Sorts items into categories using labeled chests.
archives_base_name=sortcraft

# Architectury Loom (version-independent)
architectury_loom_version=1.11-SNAPSHOT
enabled_platforms=fabric,neoforge

# Fabric Loader (version-independent)
fabric_loader_version=0.16.14

# Dependencies (version-independent)
snakeyaml_version=2.2

# NOTE: Version-specific properties are loaded from versions/<version>/gradle.properties:
# - minecraft_version
# - fabric_minecraft_version (Fabric semver-style range, e.g., ">=1.21.5 <1.21.6")
# - neoforge_minecraft_version (NeoForge Maven-style range, e.g., "[1.21.5,1.21.6)")
# - architectury_version
# - fabric_api_version
# - neoforge_version
