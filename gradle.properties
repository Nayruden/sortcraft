# Gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.daemon=true

# Mod Properties (version-independent)
mod_id=sortcraft
mod_name=SortCraft
mod_version=1.3.1
mod_group=net.sortcraft
mod_author=Nayruden
mod_description=Sorts items into categories using labeled chests.
archives_base_name=sortcraft

# Build Plugins (version-independent)
# Use net.fabricmc.fabric-loom for MC 26.1+ (non-obfuscated)
fabric_loom_version=1.16.0-alpha.16
neoforge_moddev_version=2.0.141

# Fabric Loader (version-independent)
fabric_loader_version=0.16.14

# Dependencies (version-independent)
snakeyaml_version=2.2

# Version-specific properties are loaded from versions/26.1/gradle.properties
